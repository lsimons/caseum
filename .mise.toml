# mise: https://mise.jdx.dev/
# pyproject.toml's `requires-python` stays authoritative.

[tools]
python = "3.14"
uv = "latest"

[env]
_.file = { path = ".env", optional = true }

[tasks.install]
description = "uv sync"
run = "uv sync"

[tasks.build]
description = "Build mkdocs site (publish_index.py + mkdocs build)"
run = ["uv run publish_index.py", "uv run mkdocs build"]

[tasks.deploy]
description = "Deploy to gh-pages (mkdocs gh-deploy --force)"
run = ["uv run publish_index.py", "uv run mkdocs gh-deploy --force"]

[tasks.ci-watch]
description = "Watch GitHub Actions CI for the current branch; exits non-zero on failure"
run = """
sleep 5
run_id=$(gh run list --branch "$(git branch --show-current)" --limit 1 --json databaseId --jq '.[0].databaseId')
if [ -z "$run_id" ]; then echo "No run found for current branch" >&2; exit 1; fi
gh run watch --exit-status "$run_id"
"""

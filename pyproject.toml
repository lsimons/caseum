[project]
name = "caseum"
version = "1.0.0"
description = "A simple approach to software architecture."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mkdocs>=1.6.1",
]

[tool.uv]
exclude-newer = "1 week"
